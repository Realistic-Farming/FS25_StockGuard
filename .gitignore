# Build output (shipped as a release asset, never committed)
FS25_StockGuard.zip

# Icon working file (icon.dds is the shipped one)
icon.png

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# Test harness deps (tools/test/ - installed by npm, never shipped)
node_modules/
